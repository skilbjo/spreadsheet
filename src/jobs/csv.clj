(ns jobs.csv
  (:require [clojure.java.io :as io]
            [clojure.tools.logging :as log]
            [windfall.util :as util])
  (:gen-class))

(defn -main [& args]
  (log/info "I don't do much... yet"))
